<?xml version="1.0" encoding="utf-8"?>
<menu
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/action_import"
        android:enabled="true"
        android:icon="@android:drawable/stat_sys_download"
        android:title="Import notebook..."
        android:visible="true"
        app:showAsAction="never|ifRoom" />
    <item
        android:id="@+id/action_delete"
        android:icon="@android:drawable/ic_popup_sync"
        android:title="Delete notebooks"
        app:showAsAction="never" />
    <item
        android:id="@+id/action_dumpjson"
        android:title="Dump JSON to log"
        android:enabled="true"
        app:showAsAction="never"/>
    <item
        android:id="@+id/loghierarchy"
        android:enabled="true"
        android:title="Log notebook hierarchy"
        app:showAsAction="never"
        />
    <item
        android:id="@+id/debugaddcollection"
        android:enabled="true"
        android:title="Add all notebooks to Collection"
        app:showAsAction="never"
        />

</menu>